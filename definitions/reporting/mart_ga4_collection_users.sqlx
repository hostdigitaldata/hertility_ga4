config {
  type: "table"
}



select 
   date,
   device,
   'all steps' as bar_dimension,
   count(distinct collection_users) as collection_users
from ${ref('stg_ga4_collection_users')}
group by 1,2,3