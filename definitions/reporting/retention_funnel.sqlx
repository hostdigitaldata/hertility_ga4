config {
  type: "table"
}

select
  date,
  device,
  funnel_step,
  source_medium,
  stitched_user_id,
  region
from ${ref('stg_page_visitors')}
where funnel_step != 'other'
group by  1,2,3,4,5,6