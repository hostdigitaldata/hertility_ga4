config {
  type: "view", // Creates a view in BigQuery. Try changing to "table" instead.
}

SELECT
   date,
   device,
   stitched_user_id,
   stitched_user_id AS account_register_page_users
FROM ${ ref('base_ga4_pages') }
WHERE REGEXP_CONTAINS(page, 'account/register')
GROUP BY 1,2,3