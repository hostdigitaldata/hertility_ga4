config {
  type: "view", // Creates a view in BigQuery. Try changing to "table" instead.
}

SELECT
   date,
   device,
   stitched_user_id,
   stitched_user_id AS add_to_box_users

FROM ${ ref('base_ga4_build_a_box') }
WHERE action like 'ADD%'

GROUP BY 1, 2, 3,4
