config {
  type: "table"
}

select
  distinct user_pseudo_id,
  'variant' as experiment,
  'non_kit' as funnel
from ${ref('stg_guest_checkout_funnel')}
where non_kit_journey_inclusion is true and exp_variant = 'variant'
and funnel_step != 'other'